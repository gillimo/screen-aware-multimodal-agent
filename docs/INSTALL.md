# Install

## CLI
- Run: python run_app.py status`n
## GUI
- Run: python run_app.py gui`n
## RuneLite Plugin (Local)
- Build in unelite_plugin.
- Use RuneLite dev plugin install to load the jar.
- Overlay reads data/overlay.txt.
- RuneLite command coachui launches the Tk companion UI.

